#include "context_probability.h"

#include <functional>

// size_t NodeHasher::operator()(const std::shared_ptr<Node>& node) const {
// 	return std::hash<std::string_view>{}(node->token);
// }

// bool operator==(const std::shared_ptr<Node>& v1, const std::shared_ptr<Node>& v2) {
// 	return v1->token == v2->token;
// }
